# minesweeper-reboom-web

Static site hosting the legal and support pages for the iOS game **Minesweeper: Reboom**,
served via GitHub Pages.

- Privacy Policy — `/privacy/`
- Terms of Service — `/terms/`

Plain static HTML (no build step). `.nojekyll` disables Jekyll processing so the
files are served as-is.
